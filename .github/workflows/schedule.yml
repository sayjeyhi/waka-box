name: Update Gist

on:
  push:
    branches: master
  schedule:
    - cron: "0 */3 * * *"

jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Install
        run: npm ci
      - name: Crwal and notify
        run: node ./index.js
        
